# 理论知识  
数组的删除更像是一种覆盖<br>
也就是说，假如我要删除a[i]，我要做的是a[i]=a[i+1]，并且吧后面每一项都这样错位....
# 例题
给你一个数组 nums 和一个值 val，你需要 原地 移除所有数值等于 val 的元素。元素的顺序可能发生改变。然后返回 nums 中与 val 不同的元素的数量。
假设 nums 中不等于 val 的元素数量为 k，要通过此题，您需要执行以下操作：
更改 nums 数组，使 nums 的前 k 个元素包含不等于 val 的元素。nums 的其余元素和 nums 的大小并不重要。<br>
返回 k。

输入：nums = [3,2,2,3], val = 3
输出：2, nums = [2,2,_,_]
解释：你的函数函数应该返回 k = 2, 并且 nums 中的前两个元素均为 2。
你在返回的 k 个元素之外留下了什么并不重要（因此它们并不计入评测）。<br>
# 题解<br>
- 暴力覆盖的双循环不用说，重点是双指针<br>
## 双指针解法<br>
设置一个快一个慢指针，快指针检索出要插入新数组的元素都有哪些，慢指针负责更新老数组<br>快指针遍历数组，非val即慢指针++,val则慢不变，也就是完成使新数组跳过检索出来元素的作用<br>
## 代码<br>
```
// 时间复杂度：O(n)
// 空间复杂度：O(1)
class Solution {
public:
    int removeElement(vector<int>& nums, int val) {
        int slowIndex = 0;
        for (int fastIndex = 0; fastIndex < nums.size(); fastIndex++) {
            if (val != nums[fastIndex]) {
                nums[slowIndex++] = nums[fastIndex];
            }
        }
        return slowIndex;
    }
};
```
## 补充：erase函数<br>
### 语法与用法<br>
支持全部容器<br>
- 语法：容器名.erase(坐标)
这道题用这个函数就是：while遍历，找到了，删，i值不加加；没找到，i++
 
